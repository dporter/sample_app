<% if signed_in? %>
<table class="front" summary="For signed in person">
  <tr>
    <td class="main">
      <h1 class="micropost">What's up?</h1>
      <%= render 'shared/micropost_form' %>
      <%= render 'shared/feed' %>
    </td>
    <td class="sidebar round">
      <%= render 'shared/user_info' %>
    </td>
  </tr>
</table>
<% else %>

<h1>Sample App</h1>
<p>FCome on join up you know you want to</p>
<%= link_to "Sign up now!", signup_path, :class => "signup_button round" %>
<% end %>